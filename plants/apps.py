from django.apps import AppConfig

# GardenConfig class is created to configure 'garden' app
class PlantsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'plants'
