from django.apps import AppConfig

# This class is used to configure the event_management app
class EventManagementConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'event_management'
